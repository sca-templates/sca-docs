---
title: prometheus
type: infra
status: active
repo: aws
tags:
  - type/infra
  - domain/infrastructure
  - stack/multi-lang
---

# Prometheus

> Central TSDB (30-day retention) scraping Vault, Consul, Postgres, Redis and Kafka Connect (Debezium CDC), with bundled exporters. UI on `http://127.0.0.1:9090` (loopback only).

## Role in the stack

A Compose stack in the `infra-prometheus` repo, started with `make all`:

- **prometheus** — `prom/prometheus:v2.53.0`, 30-day retention, `--web.enable-lifecycle`.
- **postgres-exporter** — `:9187`, connects to `postgres-app-db:5432`.
- **redis-exporter** — `:9121`, reaches Redis via `host.docker.internal:6379`.
- **kafka-connect-exporter** — `:9309`, JMX metrics of Kafka Connect; image `bitnamilegacy/jmx-exporter:0.20.0` (the `bitnami/` one no longer exists on Docker Hub). The broker job (`:9308`) stays commented.

## Networking

- **Prometheus runs on the host network** (`network_mode: host`) and binds its UI/API to loopback only (`--web.listen-address=127.0.0.1:9090`). This is required because the host firewall (UFW, default deny) drops Docker-bridge → host-network traffic — Consul (on `network_mode: host`) would be unreachable otherwise. Every scrape target is therefore a published `127.0.0.1:<port>`.
- The **exporters** join the external `kafka-network`: postgres-exporter resolves `postgres-app-db:5432`, kafka-connect-exporter resolves `kafka-connect:8778` (JMX).
- **Redis** is not on `kafka-network`; redis-exporter reaches it via `host.docker.internal` (`extra_hosts: host-gateway`).
- The Vault token is mounted read-only from `vault/data/secrets/root-token.txt` → `/etc/prometheus/vault-token`; after a Vault re-init, re-run `make up`.

## Scrape targets

All targets are `127.0.0.1:<port>` (published loopback) except `kong` (host-network name, tolerated down until the stack exists):

| Job | Target | Notes | Production |
|---|---|---|---|
| prometheus | `127.0.0.1:9090` | self-scrape | Same image |
| vault | `127.0.0.1:8201` | `https`, bearer token; strict — the `vault/` HCL sets `telemetry.prometheus_retention_time` | Same |
| consul | `127.0.0.1:8500` | `/v1/agent/metrics?format=prometheus` | Same |
| kong | `kong:8001` | tolerated down (no stack yet) | N/A |
| postgres-exporter | `127.0.0.1:9187` | `pg_*` | Same image |
| redis-exporter | `127.0.0.1:9121` | `redis_*` | Same image |
| kafka-connect | `127.0.0.1:9309` | JMX; needs `JMXPORT=8778` on `kafka-connect` | Same image |

Commented until a real target exists: broker kafka-exporter (`127.0.0.1:9308`) and microservices (`127.0.0.1:9101`–`9104`).

## Access

| Endpoint | Local (development) | Notes | Production |
|---|---|---|---|
| UI + API | `http://127.0.0.1:9090` | loopback only | Same image |
| Health | `http://127.0.0.1:9090/-/healthy` | container healthcheck | Same |
| postgres-exporter | `http://127.0.0.1:9187` | | Same image |
| redis-exporter | `http://127.0.0.1:9121` | | Same image |
| kafka-connect-exporter | `http://127.0.0.1:9309` | JMX over HTTP | Same image |

Credentials source: `.env` generated by `prometheus/scripts/gen-env.sh` from Vault `secret/postgres-app/dev` + `secret/redis/dev` (read via the `prometheus` AppRole).

## Metrics

Prometheus exposes its own metrics at `http://127.0.0.1:9090/metrics` (self-scrape). Key metrics for monitoring the monitoring stack itself:

| Metric | Type | What it measures |
|---|---|---|
| `up` | Gauge | Target health (1 = scraping successfully) |
| `prometheus_tsdb_head_series` | Gauge | Active time-series in memory |
| `prometheus_tsdb_head_chunks` | Gauge | TSDB chunks in memory |
| `prometheus_tsdb_head_samples_appended_total` | Counter | Samples appended to TSDB |
| `prometheus_scrape_duration_seconds` | Gauge | Duration of each scrape |
| `prometheus_scrape_samples_scraped` | Gauge | Samples scraped per target |
| `prometheus_config_last_reload_successful` | Gauge | Config reload status |
| `prometheus_rule_evaluation_duration_seconds` | Gauge | Alert rule evaluation time |
| `prometheus_notifications_sent_total` | Counter | Alerts sent to receivers |

Dashboard: [[observability#Dashboard map|prometheus.json]]

## Consul registration

[[consul]] registers prometheus and the exporters with TCP checks on `127.0.0.1:<port>`: `prometheus:9090`, `postgres-exporter:9187`, `redis-exporter:9121`, `kafka-connect-exporter:9309`.

## Observability roadmap

- [ ] Dashboard `prometheus.json`
- [ ] QA targets in `prometheus.yml`

## Target platform

In-cluster ([[adr-001-kubernetes-platform]]): Prometheus scrapes workloads natively inside the cluster and gains the Linkerd network golden signals per pair of services ([[linkerd]]). Grafana completes the four-pillar view with [[loki]] and [[tempo]] datasources ([[observability]]).

## Pointers

- Component README: [prometheus](https://github.com/sca-templates/prometheus)
- Architecture: [prometheus/docs/architecture.md](https://github.com/sca-templates/prometheus/blob/main/docs/architecture.md)
- Related notes: [[self-hosted-stack]] · [[observability]] · [[vault]] · [[consul]] · [[postgres]] · [[redis]] · [[kafka]]

## Status

Active — runs as part of the [[self-hosted]] stack.
